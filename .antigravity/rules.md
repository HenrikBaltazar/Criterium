# USER RULES & DIRECTIVES

- **STRICT GIT PUSH POLICY**: NEVER execute `git push` automatically or without explicit user authorization! Always ask the user for permission first before running any `git push` command.
